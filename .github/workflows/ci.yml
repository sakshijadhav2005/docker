name:CI

jobs:   
       build-and-push:
               runs-on: ubantu-latest  #Github Runner
               steps:
                      - uses: actions/checkout@v4  #clone the code
                      - uses: docker/setup-buildx-action@v4