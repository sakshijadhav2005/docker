name: CI

on: # when developer pushes code, pipeline gets triggered
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest # GitHub Runner

    steps:
      - uses: actions/checkout@v4 # clone the code

      - uses: docker/setup-buildx-action@v3